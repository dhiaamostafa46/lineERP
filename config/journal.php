<?php

/**
 * Journal Entry Configuration
 * مسار الملف: config/journal.php
 *
 * بعد إنشاء الملف، قم بتشغيل:
 * php artisan config:cache
 */

return [

    /*
    |--------------------------------------------------------------------------
    | استخدام AJAX للبحث الديناميكي
    |--------------------------------------------------------------------------
    |
    | true = استخدام AJAX (موصى به للبيانات الكثيرة +1000 سجل)
    | false = تحميل جميع البيانات مرة واحدة (للبيانات القليلة -1000 سجل)
    |
    */
    'use_ajax_select' => env('JOURNAL_USE_AJAX_SELECT', true),

    /*
    |--------------------------------------------------------------------------
    | إعدادات AJAX
    |--------------------------------------------------------------------------
    */
    'ajax' => [
        // عدد النتائج لكل صفحة
        'page_size' => env('JOURNAL_AJAX_PAGE_SIZE', 30),

        // مدة الانتظار قبل البحث (milliseconds)
        'delay' => env('JOURNAL_AJAX_DELAY', 250),

        // تفعيل الـ cache
        'cache' => env('JOURNAL_AJAX_CACHE', true),
    ],

    /*
    |--------------------------------------------------------------------------
    | إعدادات التحقق من البيانات
    |--------------------------------------------------------------------------
    */
    'validation' => [
        // الحد الأدنى لعدد الصفوف
        'min_rows' => 2,

        // هامش التوازن المقبول (0.01 = فرق 1 فلس مقبول)
        'balance_threshold' => 0.01,

        // الحد الأقصى للمبلغ
        'max_amount' => 999999999.99,
    ],

    /*
    |--------------------------------------------------------------------------
    | إعدادات واجهة المستخدم
    |--------------------------------------------------------------------------
    */
    'ui' => [
        // عدد الصفوف الافتراضية
        'default_rows' => 2,

        // مدة الانتظار قبل تحديث الحسابات (milliseconds)
        'debounce_delay' => 300,

        // عرض أرقام الصفوف
        'show_row_numbers' => true,
    ],
];
